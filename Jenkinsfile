pipeline {
    agent {lable 'agentlinux'}
  
    stages {        
            stage('check version') {
              steps {
                sh "node --version"
                sh "node --version"
              }
            }
        }
    }
